import FILTER_DEFAULT_VALUE from './filterDefaultValue'

const initialFilter = {
  color: FILTER_DEFAULT_VALUE,
  country: FILTER_DEFAULT_VALUE,
  grape: FILTER_DEFAULT_VALUE
}

export default initialFilter
