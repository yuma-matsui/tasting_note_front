import { useContext } from 'react'
import { TastingSheetsContext } from '../../contexts'

const useTastingSheetsContext = () => useContext(TastingSheetsContext)

export default useTastingSheetsContext
