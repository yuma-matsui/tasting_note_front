import { UseFormRegister } from 'react-hook-form'
import WineFormState from '../wine/wineFormState'

type WineMemoTextAreaProps = {
  register: UseFormRegister<WineFormState>
}

export default WineMemoTextAreaProps
